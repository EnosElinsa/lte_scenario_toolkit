# Command-line entry points

Future thin command-line wrappers will live here. They will call reusable functions from `src/` and accept `--config`, `--city` and `--output-dir` arguments.

Current entry scripts remain at the repository root until the migration phase adds compatibility wrappers.
