"""Local browser interface for the LTE scenario toolkit.

Importing this package never imports the optional NiceGUI dependency.  Callers
that only need settings, translations, or command-line parsing can therefore
use those modules with the core package installation.
"""
