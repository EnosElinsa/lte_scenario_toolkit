# Example data

Small, license-cleared fixtures for automated tests and documentation will be stored here. Full base-station points remain in `points_shp/` under Git LFS; full DEM files remain external under `dem/`.
